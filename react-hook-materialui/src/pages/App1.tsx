import React from 'react';
import FormProviderPage from './formprovider';

interface Props {}
const App1 = (props: Props) => {
  return (
    <div>
      <FormProviderPage />
    </div>
  );
};

export default App1;
